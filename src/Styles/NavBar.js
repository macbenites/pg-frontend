import styled from "styled-components";

export const NavStyle = styled.div`
    display : flex;
    flex-direction : row;
    padding: 1rem 1rem;
    p {
        font-size: 0.8rem;
        font-weight: 100;
        padding-left: 25px;
        color: white
    }
`

export const pNavStyle = styled.p`
    padding: 1rem 1rem;
`