import styled from "styled-components";

export const StyledLogin = styled.div`
  display: flex;
  align-items: center;
  flex-wrap: nowrap;
  font-size: 1.3rem;
  color: var(--text-color);
  span {
    margin-left: 0.5rem;
  }
`;